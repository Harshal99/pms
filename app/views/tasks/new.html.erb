<%= form_for([@project,@task]) do |f| %>
	<%#= f.select(:project_id,1..@project.count) %>
	<%= f.hidden_field(:project_id,:value =>@project.id) %>
	<%= f.text_field(:tname) %>
	<%= f.text_field(:tdescrtion) %>
	<%= f.submit("Create task") %>
<% end %>
